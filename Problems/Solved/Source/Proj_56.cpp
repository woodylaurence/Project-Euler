/*
 * Proj_56.cpp
 *
 *  Created on: Aug 19, 2012
 *      Author: Laurence
 *
 *      Problem: Find maximum digital sum of a^b for 1 <= a,b <= 100
 *
 *      NOTE: REQUIRES bigInt.cpp TO RUN
 */

#include "BigInt.hpp"

class Prob56
{
	int main(void)
	{
        throw "Needs reimplementing";
		/*int maxDigitalSum = 0;
		BigInt tmpNumber;

		for (int a = 2; a <= 100; a++)
		{
			cout << a << endl;
			for (int b = 2; b <= 100; b++)
			{
				tmpNumber = BigInt::power(a, b);

				if (tmpNumber.sumDigits() > maxDigitalSum)
				{
					maxDigitalSum = tmpNumber.sumDigits();
				}
			}
		}

		cout << "Maximum digital sum: " << maxDigitalSum << endl;

		return 0;*/
	}
};
